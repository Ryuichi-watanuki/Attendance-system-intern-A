<% provide(:title, 'ユーザー') %>
<h1>全てのユーザー</h1>

<div class="row">
  <div class="search_form">
    <%= render 'users/search_form' %>
  </div>
</div>

<%= will_paginate %>

<% unless @users.empty? %>
  <ul class="users">
    <%= render @users %>
  </ul>
<% else %>
  <p>
    お探しのユーザーは見つかりませんでした。<br>
    再度入力し直してみてください。
  </p>
<% end %>

<%= will_paginate %>